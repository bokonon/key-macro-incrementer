'use babel';

import KeyMacroIncrementerView from '../lib/key-macro-incrementer-view';

describe('KeyMacroIncrementerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
